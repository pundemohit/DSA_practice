#include<iostream>
using namespace std;

int main(){

    // char ch = 'q';
    // cout<< sizeof(ch)<<endl;

    // char* c = new char;
    // cout<< sizeof(c)<<endl;
    int n;
    cout<< "Enter the size of array";
    cin>>n;

    int * arr = new int[n];

    for (int i = 0; i < n; i++)
    {
        
    }
    

    return 0;
}